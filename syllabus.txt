(ChatGPT's Syllabus Instructions)
If I were doing this project myself, I'd implement the Functions in roughly this order:
--LinearFunction (wx_plus_b)
--ReLUFunction
--SoftmaxFunction
--CrossEntropyFunction
--LayerNormFunction
--EmbeddingFunction
--Gelu
--Silu
--Mask

Then build Modules:

--Linear
--LayerNorm
--Embedding
--FeedForward
--SingleHeadAttention
--MultiHeadAttention
--MoEFeedForward
--TransformerBlock
--GPT

Then other necessary objects:
--AdamW

Then assemble into a script to train the model
    Tokenizer
    Training Loop
    Generation Loop
    Checkpoint Manager

Modules to be added in the future:
    RotaryEmbedding / RoPE
    RMSNormFunction & Layer (replacement of LayerNorm)
    --Dropout Layer
    --Gradient Clipping
    --Model Config Obj?
    Weight tying from embeddings to final linear projection to vocab size?
