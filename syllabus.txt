Modules to be added in the future:
    MoE Feedforward (working and gated with a load balancing loss)
    Modularize weight init scaling into functions

Popular Attention Subvariants (https://magazine.sebastianraschka.com/p/visual-attention-variants):
    • Read the article
    • Create abstract bases/modular interfaces for different attentions based on their taxonomy
    • Refer to and update attention syllabus and decide what to implement accordingly
    Each attention subvariant basically requires its own rope and kv caching implementation :(
        RotaryEmbedding / RoPE (the exact kind of implementations on how many dimensions needs to be seen)
        KV Caching


----------------------------------------
Things that are done:
    (ChatGPT's Syllabus Instructions)

    If I were doing this project myself, I'd implement the Functions in roughly this order:
        --LinearFunction (wx_plus_b)
        --ReLUFunction
        --SoftmaxFunction
        --CrossEntropyFunction
        --LayerNormFunction
        --EmbeddingFunction
        --Gelu
        --Silu
        --Mask


    Then build Modules:
        --Linear
        --LayerNorm
        --Embedding
        --FeedForward
        --SingleHeadAttention
        --MultiHeadAttention
        --MoEFeedForward
        --TransformerBlock
        --GPT

    Then other necessary objects and functions:
        --AdamW
        --Dropout Layer
        --Gradient Clipping (cf trainer.utility.scale_down_large_gradients)
        --Model Config Obj

    Then assemble into a script to train the model
        --Tokenizer
        --Training Loop
        --Generation Loop
        --Checkpoint Manager

    Modernizations:
        -- RMSNorm Function & Layer (replacement of LayerNorm)
        -- Gated Feed Forward
        -- Weight tying (implemented as part of GPTModel as an optional param with comments explaining it at GPTModel.__init_)